<h1>Payment</h1>

<p>UCBMUN uses Stripe for secure payments. No credit card information will
  be saved by UCBMUN, but may be saved by Stripe with your permission. A
  reciept of your payment will be emailed to your provided email. Your credit card
  payment will show as charged by UCBMUN.org.</p>
<%= current_user.get_current_balance %>
<%= form_tag charges_path do %>
  <article>
    <label class="amount">
      <h3>Amount: <%= "$#{current_user.get_current_balance}.00" %></span>
    </label>
  </article>

  <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
          data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
          data-description="Delegation Payment"
          data-amount=<%= @amount %>></script>
<% end %>
