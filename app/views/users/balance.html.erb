<h1> Finances </h1>
<br></br>

<div class="balance-box">
  <table>
    <th>
      <td> Date </td>
      <td style="padding: 10px;"> Item </td>
      <td style="padding: 10px;"> Qty. </td>
      <td style="padding: 10px;"> Cost </td>
    </th>
    <% @user.charge_list.each do |charge| %>
      <tr>
        <td style="padding: 10px; margin-right: 200px;"> <%= charge[:date] %>
        <td style="padding: 10px;"> <%= "#{charge[:item]} @ $#{charge[:price]}.00" %></td>
        <td style="padding: 10px;"> <%= charge[:quantity] %> </td>
        <td style="padding: 10px;"> <%= "#{charge[:price] * charge[:quantity]}.00" %></td>
      </tr>
    <% end %>
    <tr>
      <td style="padding: 10px;"></td>
      <td style="padding: 10px;"></td>
      <td style="padding: 10px;"></td>
      <td style="padding: 10px;"><%= "$#{@user.get_current_balance}.00" %></td>
    </tr>
  </table>
<h2> Your balance is: <strong> <%= " $#{@user.get_current_balance}.00" %> </strong> </h2>

<a class="btn btn-default" href="/charges/new" role="button">Pay with Stripe!</a>


<%= @user.get_current_balance %>
<%= @user.charge_list %>
<%= @user.name %>

<%= current_user.get_current_balance %>
<%= current_user.charge_list %>
<%= current_user.name %>
